export const description =
  "What if we stopped displaying buttons for everything and showed what users admit and this by default becomes the bigger text version which can still be truncated if incredibly long. This goes until the limit of the height size and if more text is needeed we then display the view more text on the right. What if we stopped displaying buttons for everything and showed what users admit and this by default becomes the bigger text version which can still be truncated if incredibly long. What if we stopped displaying buttons for everything and showed what users admit and this by default becomes the bigger text version which can still be truncated if incredibly long.";

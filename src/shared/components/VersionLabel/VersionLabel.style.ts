import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    position: "absolute",
    backgroundColor: "transparent",
    bottom: 0,
    left: 0,
    right: 0,
    paddingVertical: 5,
  },
  text: {
    textAlign: "center",
  },
});

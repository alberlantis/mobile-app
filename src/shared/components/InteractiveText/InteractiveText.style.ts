import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    fontSize: 14,
    fontWeight: "regular",
  },
  interactiveText: {
    textDecorationLine: "underline",
  },
});

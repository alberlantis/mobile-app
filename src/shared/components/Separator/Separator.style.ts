import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    width: "100%",
    justifyContent: "center",
    alignItems: "center",
  },
  line: {
    width: "100%",
    position: "absolute",
  },
  label: {
    padding: 15,
    borderRadius: 30,
  },
});

import { StyleSheet } from "react-native";
import { colors } from "src/theme";

export default StyleSheet.create({
  keyboardContainer: { flex: 1, backgroundColor: colors.BLACK },
  contentContainer: {
    flexGrow: 1,
  },
});
